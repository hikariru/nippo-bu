env BASIC_AUTH_USERNAME;
env BASIC_AUTH_PASSWORD;

server {
  listen 80;

  # ドキュメントルート
  location / {
    root /app/public;

    auth_basic "Restricted Access";
    auth_basic_user_file /etc/nginx/.htpasswd_env
  }
}
